
import { AppBar, Toolbar } from '@mui/material';

const NavBar = () => {

    return (
        <div>
            <AppBar position="fixed">
                <Toolbar>
                    WHY MUI IS AWESOME???
                </Toolbar>
            </AppBar>
        </div>
    );
}

export default NavBar;
